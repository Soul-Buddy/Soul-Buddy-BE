spring:
  profiles:
    active: local
  application:
    name: soul-buddy
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: openid, profile, email
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: ${SERVER_PORT:8080}

ai:
  clovax:
    api-key: ${CLOVAX_API_KEY}
    base-url: https://clovastudio.stream.ntruss.com/v1
    model: HCX-003
    timeout-seconds: 30
    max-retries: 2

jwt:
  secret: ${JWT_SECRET}
  expiration-ms: 86400000  # 24h

cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:3000}
  frontend-url: ${FRONTEND_URL:http://localhost:3000}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html


